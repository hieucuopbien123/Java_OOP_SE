import testclass.testclass;//package nằm trong 1 thư mục con cùng tên package

public class Package {
    // Basic - package
    public static void main(String[] args) {
        testclass sw = new testclass();
        sw.printFunction();
    }
}